# API base URL used by the frontend (include /api path)
# Local dev with Vite proxy:
VITE_API_URL=/api
VITE_API_PROXY_TARGET=http://localhost:4000

# Production (point at your deployed API):
# VITE_API_URL=https://api.yourdomain.com/api
